import React, { PureComponent } from 'react';
import PropTypes from 'prop-types';

class InputText extends PureComponent {
  render() {
    return (
      <div>[*Component is InputText*]</div>
    );
  }
}

InputText.propTypes = {};
InputText.defaultProps = {};

export default InputText;
