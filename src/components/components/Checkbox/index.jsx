import React, { PureComponent } from 'react';
import PropTypes from 'prop-types';

class Checkbox extends PureComponent {
  render() {
    return (
      <div>[*Component is index*]</div>
    );
  }
}

Checkbox.propTypes = {};
Checkbox.defaultProps = {};

export default Checkbox;
